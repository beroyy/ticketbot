{
  "name": "ticketsbot-ai",
  "version": "0.0.1",
  "description": "Discord ticket bot with web dashboard",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf node_modules/.cache/turbo",
    "db:generate": "dotenv -- pnpm --filter @ticketsbot/db db:generate",
    "db:migrate": "dotenv -- pnpm --filter @ticketsbot/db db:migrate",
    "db:push": "dotenv -- pnpm --filter @ticketsbot/db db:push",
    "db:push:force": "dotenv -- pnpm --filter @ticketsbot/db db:push:force",
    "db:seed": "tsx packages/scripts/src/db/seed.ts",
    "db:studio": "dotenv -- pnpm --filter @ticketsbot/db db:studio",
    "db:reset": "dotenv -- pnpm --filter @ticketsbot/db db:reset",
    "dev": "tsx packages/scripts/src/dev/start-dev.ts",
    "docker": "docker compose up --build",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\"",
    "lint": "turbo run lint -- --fix",
    "start": "turbo run start",
    "typecheck": "turbo run typecheck"
  },
  "dependencies": {
    "zod": "^4.0.5"
  },
  "overrides": {
    "zod": "^4.0.5",
    "@sapphire/framework": "^5.3.6",
    "discord.js": "^14.21.0",
    "prisma": "^6.12.0",
    "@prisma/client": "^6.12.0",
    "@prisma/adapter-neon": "^6.12.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.11",
    "@types/node": "^24.0.14",
    "concurrently": "^9.2.0",
    "dotenv-cli": "^9.0.0",
    "eslint": "^9.31.0",
    "neonctl": "^2.12.0",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "tsx": "^4.20.3",
    "turbo": "^2.5.5",
    "typescript": "^5.8.3"
  }
}
