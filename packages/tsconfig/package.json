{
  "name": "@ticketsbot/tsconfig",
  "version": "0.0.1",
  "description": "Shared TypeScript configurations for TicketsBot monorepo",
  "private": true,
  "scripts": {
    "typecheck": "echo 'No TypeScript files to check'",
    "lint": "echo 'No JavaScript files to lint'"
  },
  "files": [
    "base.json",
    "node.json",
    "nextjs.json",
    "library.json"
  ],
  "devDependencies": {
    "@types/node": "^24.0.14"
  }
}
